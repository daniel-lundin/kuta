const test = require('../lib/main.js').test;

test('a simple test', () => {
  console.log('running a simple test');
});

test('another simple test', () => {
  console.log('running another simple test');
});
